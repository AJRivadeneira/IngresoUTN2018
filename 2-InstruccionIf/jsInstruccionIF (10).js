function Mostrar()
{
	//Genero el número RANDOM entre 1 y 10 
	var numAlt
	
	numAlt = Math.floor(Math.random() * 10) + 1

	if(numAlt == 10 && 9)
	{
		alert("Excelente")
	}
	else if(numAlt >4 )
	{
		alert("Aprobo")
	}
	else
	{
		alert("Vamos, la proxima se puede")
	}



}//FIN DE LA FUNCIÓN